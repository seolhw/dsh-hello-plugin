{
  "name": "@dsh-talk/server",
  "version": "0.0.1",
  "private": true,
  "description": "dsh-talk Cloudflare Hub: Hono Worker + D1 + R2 + Durable Object",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:up": "drizzle-kit up",
    "db:drop": "drizzle-kit drop",
    "db:check": "drizzle-kit check",
    "db:studio": "drizzle-kit studio",
    "db:apply-local": "wrangler d1 migrations apply talkhub --local",
    "db:apply-remote": "wrangler d1 migrations apply talkhub --remote",
    "test": "echo TODO"
  },
  "dependencies": {
    "@dsh-talk/types": "workspace:*",
    "drizzle-orm": "^0.38.0",
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "drizzle-kit": "^0.30.0",
    "typescript": "^7.0.2",
    "wrangler": "^3.90.0"
  },
  "license": "MIT"
}
