# 复制为 .dev.vars 并填入真实值（或直接写到仓库根 .env，pnpm dev 时 predev 会自动同步）。
# 只放密钥/凭据，其它策略都是代码常量（见 src/lib/auth.ts / email.ts）。不要提交真实密钥。
# 生产环境请用 `wrangler secret put` 逐项设置。

# ---- Better Auth（必填） ----
BETTER_AUTH_SECRET="change-me-to-a-random-string-at-least-32-chars"

# ---- GitHub OAuth（配置后才显示 GitHub 登录） ----
# OAuth App 回调地址填：http://localhost:8787/api/auth/callback/github（生产填你的域名）
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# ---- Resend 事务邮件（邮箱验证 / 找回密码） ----
RESEND_API_KEY=""
